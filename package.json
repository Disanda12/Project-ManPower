{
  "private": true,
  "scripts": {
    "dev": "concurrently --kill-others-on-fail false \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "build": "npm run build --prefix frontend",
    "start": "npm start --prefix backend",
    "prod": "npm run build && npm run start"
  },
  "dependencies": {
    "multer": "^2.0.2"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
